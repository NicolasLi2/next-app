'use client';
export default function AddToCart() {
  return (
    <div>
      <button className='btn btn-primary' onClick={() => console.log('Click')}>
        Button
      </button>
    </div>
  );
}
